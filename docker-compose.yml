services:
  card-studio:
    image: card-studio-frontend:latest 
    container_name: card-studio-front-prod

    restart: always
    build:
      context: .
      target: dev
    ports:
      - "8080:80"  
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - WATCHPACK_POLLING=true
 
 
